# Handoff - P0-01 Bootstrap pnpm monorepo

- Trạng thái: `DONE`
- Ngày/người thực hiện: 2026-07-13 / Codex
- Commit/release/worktree: Workspace chưa có metadata Git; bằng chứng dùng current filesystem và clean-copy verification.
- Requirements/checklist/decisions: `P0-01`, `GOV-001..005`, `GOV-007`, `P0-001..008`, `D-001..D-008`, `D-022..D-024`.

## Outcome

Monorepo pnpm đã có đúng 3 app và 6 package theo D-003. Một bản sao sạch có thể cài bằng lockfile đã khóa, chạy toàn bộ quality gate và build production. Web, realtime và worker đều có health entrypoint hoạt động.

## Thay đổi

- Root toolchain: Node `24.14.1`, pnpm `11.12.0`, TypeScript `6.0.3`, ESLint `9.39.5`, Prettier `3.9.5`, Vitest `4.1.10`.
- Web stack: Next.js/eslint-config-next `16.2.10`, React/React DOM `19.2.7`, Tailwind CSS `4.3.2`, Zod `4.4.3`.
- Tạo `apps/web`, `apps/realtime`, `apps/worker` và `packages/domain`, `db`, `ui`, `contracts`, `config`, `testkit`.
- Tạo TypeScript strict configs, project references, pnpm-only preinstall, repository guard, formatting/lint/test/build scripts và env schemas theo process.
- Tạo README chạy local, `.env.example`, infra directory boundaries và clean-copy verifier.
- Không triển khai PostgreSQL, Redis, Prisma, CI, Playwright project harness, Socket.IO, BullMQ hoặc feature P1+.

## Acceptance evidence

| Tiêu chí | Bằng chứng | Kết quả |
|---|---|---|
| Pnpm/version/lock/workspace đúng | `corepack pnpm guard:plan` | PASS |
| Frozen install | `corepack pnpm install --frozen-lockfile` | PASS, pnpm 11.12.0 |
| Formatting/lint/strict typecheck | `corepack pnpm verify` | PASS |
| Unit/guard/env/startup/clean-copy policy | 6 test files, 23 tests | PASS |
| Project references gồm web | `corepack pnpm exec tsc -b --pretty false --force` | PASS |
| Production build | Next routes `/`, `/health`, `/api/health`; server apps compile | PASS |
| Clean-copy install + full verify | `corepack pnpm verify:clean` | PASS, exit 0 |
| Runtime health | HTTP 200 cho web page/API, realtime và worker | PASS |
| Desktop/mobile visual smoke | `P0-01-web-desktop.png`, `P0-01-web-mobile.png`; không console error | PASS |

## Commands đã chạy

```text
corepack prepare pnpm@11.12.0 --activate                         PASS
corepack pnpm install --frozen-lockfile                         PASS (exit 0)
corepack pnpm guard:plan                                        PASS
corepack pnpm exec tsc -b --pretty false                        PASS (exit 0)
corepack pnpm verify                                            PASS (exit 0)
corepack pnpm verify:clean                                      PASS (exit 0)
corepack pnpm audit:prod                                        PASS (0 known vulnerabilities)
python <temporary Playwright smoke script>                      PASS
Invoke-WebRequest http://localhost:3000/                        HTTP 200
Invoke-WebRequest http://localhost:3000/health                  HTTP 200
Invoke-WebRequest http://localhost:3000/api/health              HTTP 200
Invoke-WebRequest http://localhost:3001/health                  HTTP 200
Invoke-WebRequest http://localhost:3002/health                  HTTP 200
```

Playwright smoke script chỉ là công cụ QA tạm thời và đã được xóa; ảnh bằng chứng được giữ trong thư mục handoff.

## Migration/deploy/rollback

- Migration/data impact: Không có database hoặc migration ở P0-01.
- Config/secret mới: `.env.example` chỉ chứa placeholder/default, không có secret thật.
- Deploy order: Chưa deploy; Docker/staging thuộc P0-02/P0-03.
- Rollback: Xóa các workspace/toolchain files mới; không có dữ liệu cần chuyển đổi.

## Security/privacy/performance

- Dependency build scripts được allowlist rõ; PostCSS được override lên bản vá `8.5.18`; frozen lockfile, supply-chain policy và production audit đều pass.
- `guard:plan` có negative tests cho foreign lockfile, sai package manager, workspace thừa, task active không có trong roadmap và forbidden domain import ở thư mục lồng/subpath/dynamic import.
- Health payload chỉ có `service`, `status`, `timestamp`; không lộ version, secret hoặc PII.
- Ba process nạp root `.env` rồi validate; web có preflight bắt buộc trước dev/build/start/typecheck. Invalid URL/port và startup failure có regression tests.
- Clean-copy policy loại `.env*`, `data`, log và local/generated artifacts trước khi copy vào `%TEMP%`; cleanup path được kiểm tra.

## Rủi ro/phần còn lại

- Chưa có metadata Git/remote, nên clean-copy verification thay cho literal clone/commit evidence.
- PostgreSQL/Redis/readiness/migration thuộc `P0-02`.
- CI, full test harness, structured logging/correlation ID và Playwright E2E project thuộc `P0-03`.
- Local Windows có pnpm global 9.15.9 đứng trước Corepack trong PATH; README và root scripts gọi `corepack pnpm` để luôn dùng bản 11.12.0 đã khóa.

## Trạng thái kế hoạch

- Checklist đã tick theo bằng chứng: `GOV-001..005`, `GOV-007`, `P0-001..008`.
- `PROJECT_STATE.md`: `P0-01` hoàn tất; P0 vẫn đang thực hiện.
- Task duy nhất được phép tiếp theo: `P0-02`.

## Adversarial review closure

- Env startup/root `.env`: đã sửa và có process-level regression test.
- Domain boundary recursive/subpath imports: đã sửa và có negative test.
- Clean-copy secret/local artifact leakage: đã sửa và có policy tests.
- PostCSS moderate advisory: đã override lên `8.5.18`; audit/build/clean-copy pass.
- Web project-reference gap: đã thêm `apps/web/tsconfig.build.json`; `tsc -b --force` pass.
